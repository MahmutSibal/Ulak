# Dev (same machine / same LAN): leave empty and use Vite proxy (/api -> http://localhost:8000)
# VITE_API_BASE_URL=

# Different network / Production: point to your public backend base URL
# Example:
# VITE_API_BASE_URL=https://api.example.com
